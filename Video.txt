# How To Model Your DB :
- explain Read To write ratio in exam's questions => 
  Rule 5 here : 
  https://www.mongodb.com/blog/post/6-rules-of-thumb-for-mongodb-schema-design-part-2 => search on frequent , ratio 
- compound index + idea of fetch lesson with didStudy


# Bassthalk 3 :
- Explain how to 'draw screen in bassthalk' by 2 groups pipelines

{
  $group:{
    _id:'$user',   [{user:'remah',points:3},{user:'hamoda',points:2}]
    points: { $sum: '$point'}
  },
  $group:{
    _id: '$points', [{_id:55,numOfUsers:3},{},{}]
    numOfUsers: { $sum:1}
  }
}


# Creation Of Admin Idea :
- explain admin idea

# Relations :
- explain all models , note : validation ok in course.model file
- in courseModel I put Record like in docs, but in dto I put class to validate it,
- in relation if you put string , class , classOrString => explain (make type string , if you need make union type like at unit.model)
- note that some people when use class not classDocument , they extended Document in that class
- toJSON()
- validation on subschema is applied ?

# Bassthalk_NEST
- explain cycle , with limitations (like no search for id send to make sure)

# Structure : 
-  when naming make name of classes you create like dto, model,.. Single , also make name of model & repo files single , rest of files make it plural ,
   see mongo in nest docs , typegoose , https://github.dev/angular-university/nestjs-course/tree/master/rest-api/src , 'no specific way to do that',
 
- Note to reason of : rename paramsOrQueryWithId to paramsWithId , also make attributes required not optional 


- explain BaseModel usage with limitations (no read toJson(),also must put @schema in child)

- id,createdAt,updatedAt in schema not important => its only for swagger 

- refer to lesson schema with optional attributes

- note to startSolution in exams.service when return nothing , and explain if you remove return , then you throw error it will works incorrectly

- note to pick/omit work successfully

- explain findall in units if you return only no paginated response and you want to use  findAllWithPaginationOption in Base
- also explain paramater of function in that case

- explain create lesson

- refer to Gaurds in ts , ? , ! 

- explain new decorator in dto + refer to class-validator docs

- note that some people when use class not classDocument , they extended Document in class

- when update class , update all related names 'explain your idea'

- explain idea of convert params to query in find 'easier , no need to create more classes'

- fetch all courses repo

- explain 2 methods of permissions

- explain file params  in dto/exams


- explain base.repository.ts 
- handle select in update and test , see if you must select in updateOneVoid ?
- params of createRepo & updateRepo
- dont forget to add multerModule if going to upoload files
- explain routing

- explain steps :
    model
    repository
    module
    dto
    service -> 
    controller -> 
    return types
    swagger tags on conctroller
    in case of fetchAll then add file filterQueryOptions in dto folder
    SWAGGER with all cases

# General :
- refactor all code


# SWAGGER :
- 'make sure emeded classes at nested schemas appear in swagger'
- explain disadvantage of raw() in model becuase you can't take advantage to swagger by ref model
- handle function swagger for pagination or not $change in master$ , refer to :https://docs.nestjs.com/openapi/types-and-parameters#oneof-anyof-allof
- handle function with all cases , https://docs.nestjs.com/openapi/operations#responses
- explain apihideProperty with default value case 'like seen , unseen', => 
    @apihideProperty()
    status:enum = enum.seen
- handle in return profile if return user + (student OR teacher)
$ send image with object Or array 'in create class' with swagger , https://github.com/scottie1984/swagger-ui-express/issues/57
- explain function you created with autocomplete
- refer to steps
- note to more enhance like send status with ApiResponse 
- https://swagger.io/
